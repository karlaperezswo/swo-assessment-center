// AWS Landing Zone seed. Source-of-truth lives in shared/data/landingZoneSeeds.ts
// so the frontend can render the same checklist without duplication.

import { LANDING_ZONE_SEEDS } from '../../../../../shared/data/landingZoneSeeds';

export const awsLandingZoneSections = LANDING_ZONE_SEEDS.aws;
